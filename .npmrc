# @tscircuit/schematic-viewer declares `tscircuit: "*"` as a peer dependency;
# without this flag npm >=7 auto-installs the entire tscircuit core package,
# which we deliberately do not depend on (see docs/decisions/0001).
legacy-peer-deps=true
